spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    enabled: true
  cache:
    type: redis

logging:
  level:
    com.appname.userservice: DEBUG